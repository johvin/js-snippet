# object-inner-function-call
==============

### description

when an object inner function is called not as the object attribute, "this" is bounded to the global object.
this is a language design fault.

the code will verify the fault.